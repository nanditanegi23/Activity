DROP TABLE IF EXISTS conversion;
CREATE TABLE conversion ( countryCode  VARCHAR(10) PRIMARY KEY, conversionFactor decimal(30,2) NOT NULL);  
INSERT INTO product (countryCode, conversionFactor) VALUES
('USD', 60),
  ('EUR', 80),
  ('Yen', 2);